 minimal rocky linux\
terraform \
git installed\
useradd terraform \
passwd terraform\
login as terrafom user\
create a webserver and upload the image rhel.qcow2 to the webserver.\
ensure the qcow2 image can be downloaded form the webserver.\
mkdir -p ~/terraform\
 clone https://github.com/nutanixdev/terraform_blog\
  ~/nutanix/terraform/terraform_blog/intro\
terraform init\
update 
 terraform.tfvars with cluster_name,subnet_name(VM VLAN),user
 (prism central user),
 password(pc password),endpoint(IP/FQDN)

upload RHELqcow2 image to the prism central images.\
